extends: train_a100_base

experiment:
  name: ${EXP_NAME_ABL}

train:
  checkpoint_dir: ${CKPT_DIR_ABL}

loss:
  match_weight: 0.0
  composition_weight: 0.0
  defect_weight: 0.0
  warp_weight: 0.0
  boundary_weight: 1.0

boundary:
  enable_until_step: 500000
